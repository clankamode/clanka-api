{
  "name": "clanka-api",
  "version": "1.0.0",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.3",
    "vitest": "^4.0.18",
    "wrangler": "^3.28.1"
  }
}
